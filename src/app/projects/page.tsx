import { ProjectsContent } from "@/components/public-pages";
import { PublicShell } from "@/components/public-shell";

export default function ProjectsPage() {
  return (
    <PublicShell>
      <ProjectsContent />
    </PublicShell>
  );
}
