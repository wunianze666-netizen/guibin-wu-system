import { ResumeContent } from "@/components/public-pages";
import { PublicShell } from "@/components/public-shell";

export default function ResumePage() {
  return (
    <PublicShell>
      <ResumeContent />
    </PublicShell>
  );
}
