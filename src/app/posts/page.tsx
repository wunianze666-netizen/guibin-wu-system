import { PostsContent } from "@/components/public-pages";
import { PublicShell } from "@/components/public-shell";

export default function PostsPage() {
  return (
    <PublicShell>
      <PostsContent />
    </PublicShell>
  );
}
