import { MessagesContent } from "@/components/public-pages";
import { PublicShell } from "@/components/public-shell";

export default function MessagesPage() {
  return (
    <PublicShell>
      <MessagesContent />
    </PublicShell>
  );
}
